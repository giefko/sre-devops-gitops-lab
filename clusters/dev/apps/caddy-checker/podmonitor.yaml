apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: caddy-checker
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - apps
  selector:
    matchLabels:
      app: caddy-checker
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
