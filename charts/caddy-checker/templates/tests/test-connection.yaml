apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caddy-checker.fullname" . }}-test-connection"
  labels:
    {{- include "caddy-checker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caddy-checker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
