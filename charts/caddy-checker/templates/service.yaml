apiVersion: v1
kind: Service
metadata:
  name: {{ include "caddy-checker.fullname" . }}
  labels:
    {{- include "caddy-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caddy-checker.selectorLabels" . | nindent 4 }}
